import "./index.css";
import { QuestionBodyProps } from "../../../../types/types";

/**
 * The component is used to render the contents of the question being answered.
 * @param {QuestionBodyProps} props - The props to be passed to the component.
 * @returns JSX.Element containing the question body.
 */
const QuestionBody = () => {
  return (
    <div id="questionBody">
      Question Body
    </div>
  );
};

export default QuestionBody;
