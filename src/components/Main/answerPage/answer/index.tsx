import { AnswerProps } from "../../../../types/types";
import "./index.css";

/**
 * The component renders an answer, the username of the user
 * who answered it, the date it was answered.
 * @param {AnswerProps} props - The props to be passed to the component.
 * @returns JSX.Element containing the answer.
 */
const Answer = () => {
  return (
    <div className="answer right_padding">
      Answer
    </div>
  );
};

export default Answer;
