import { AnswerPageProps } from "../../../types/types";
import "./index.css";

/**
 * Renders all answers to a question aong with the question content.
 * @param {AnswerPageProps} props - The props to be passed to the component.
 * @returns JSX.Element containing the answer page.
 */
const AnswerPage = () => {
    return (
        <>
            Answer Page for a question
        </>
    );
};

export default AnswerPage;
